"""AzureChat — a beginner-friendly generative AI chat app on Azure OpenAI.

Keep this file lightweight: importing the package should NOT pull in heavy
dependencies (openai, streamlit). That keeps the unit tests fast and offline.
"""

__version__ = "0.1.0"
